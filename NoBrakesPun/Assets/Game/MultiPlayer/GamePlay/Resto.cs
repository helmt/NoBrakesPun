using System.Collections;
using System;
using System.Collections.Generic;
using UnityEngine;

public class Resto : MonoBehaviour
{
    /*
    [SerializeField]
    private Tuple<int, int> pricerange;
    public GameObject zoneresto;
    public Resto resto;
    public Job temporary_job;

    public Resto(Resto resto, Tuple<int, int> price)
    {
        this.resto = resto;
        pricerange = price; 
    }

    public Tuple<int,int> Getpricerange()
    {
        return pricerange; 
    }

    public float Getcoordinates_x()
    {
        return zoneresto.transform.position.x; 
    }

    public float Getcoordinates_y()
    {
        return zoneresto.transform.position.y; 
    }
    public void GenerateCommande()
    {
            zoneresto.SetActive(true);
            temporary_job = new Job(pricerange, resto, null);
    }

    public bool PlayerInZone(Player player)
    {
        return (Math.Sqrt(Math.Pow((player.X() - Getcoordinates_x()), 2) +
                          Math.Pow((player.Y() - Getcoordinates_y()), 2)) <= 1);
    }
    
    public void Proposal(Player player)
    {
        zoneresto.SetActive(false);
        // do GUI stuff to propose to player
        // get input somehow
        
    }
*/
}

